{
  "name": "london-field-guide",
  "version": "0.2.0",
  "private": true,
  "engines": { "node": ">=18.0.0" },
  "scripts": {
    "fetch-places": "node scripts/fetch-places.js",
    "build": "node build.js"
  },
  "devDependencies": {
    "esbuild": "0.24.0"
  }
}
